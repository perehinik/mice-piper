#!/bin/bash
set -e

echo "[mouse-piper] Running post-install steps..."

mkdir -p "/etc/mouse-piper"
# Reload and enable service
systemctl daemon-reload || true
systemctl enable mouse-piper.service || true
systemctl restart mouse-piper.service || true

echo "[mouse-piper] Service enabled and started."
